initialize 2D vector after declaration
    
    vector<vector<int>> dp;
    dp.resize(5, vector<int>(10, -4));
---------------------------------------------------------------------------------------------
Taking complete line as Input
   
    string s;
    getline(cin, s);
    
It helps takes Entire string with spaces as input.
aabdfjs dfjlkjs fjdsjhf  poqiopjf  f  fd  dssdfsd
---------------------------------------------------------------------------------------------
priority_queue

priority_queue<int> pq; // max priority-Queue
priority_queue<int, vector<int>, greater<int>> pq; // min priority queue

Sorting
int arr[] = {4, 6, 2, 7, 9};
sort(arr, arr+5); // ascending order
sort(arr, arr+5, greater<int>()); // // Descending order

---------------------------------------------------------------------------------------------
sorting vector using sort()



---------------------------------------------------------------------------------------------
Assert in C++

#include <assert.h>
assert(x==7);

---------------------------------------------------------------------------------------------
#ifndef ANIMAL
#define ANIMAL

class Animal {
    // Code
};

#endif